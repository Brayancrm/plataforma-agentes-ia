rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGRAS ULTRA-SIMPLES PARA TESTE
    // Permitir tudo para usuários autenticados (temporário para debug)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

